import React from "react";

const Heading = (children) => {
  return (
    <div>
      <h1 className="text-[18px] font-semibold text-gray4B pb-5">{children}</h1>
    </div>
  );
};

export default Heading;
